services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: paydue
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - paydue_pg:/var/lib/postgresql/data

volumes:
  paydue_pg:
